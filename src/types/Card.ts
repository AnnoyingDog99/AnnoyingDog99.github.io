export type CardProps = {
    imageRoute: string,
    imageAlt: string,
    languages: string[],
    projectTitle: string,
    projectDescription: string,
    githubLink: string,
    liveLink?: string
}